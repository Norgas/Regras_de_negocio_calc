def subtracao(x):
    global resul
    y = int(input())
    resul = x - y
    oper = input("oper: ")
    if oper == "-" :
        subtracao(resul)
    return(resul)

a = int(input())

oper = input("operaçao: ")

if oper == "-":
    subtracao(a)

print(resul)
